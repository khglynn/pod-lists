# list-maker Pipeline Dependencies — RUNTIME only.
# Install: pip install -r requirements.txt
# Tests + dev tools: pip install -r requirements-dev.txt (includes this file).
#
# Why the split (2026-09-01): every scheduled production run installs this file, and
# test.yml runs pip-audit against it. A High advisory in pytest would fire the same
# alarm as one in psycopg2, even though pytest never touches a live credential —
# a gate that cries about dev tools is a gate people learn to ignore.
#
# Used by:
#   - run_pipeline.py (orchestrator)
#   - spotify_match.py (match songs to Spotify)
#   - sync_playlist.py (sync matched songs to playlist)
#   - scrapers/sop/scrape.py (SOP episode scraper)
#   - scrapers/tal/ (TAL scraper suite)
#   - scrapers/ai_daily/ (AI Daily entity extraction)

spotipy>=2.26.0           # Spotify API client
psycopg2-binary>=2.9.12    # PostgreSQL driver (Neon)
python-dotenv>=1.0.0      # Load .env files
thefuzz>=0.22.1           # Fuzzy string matching
python-Levenshtein>=0.27.4  # 10x faster fuzzy matching
httpx>=0.28.1             # Async HTTP client (for Firecrawl)
requests>=2.31.0          # RSS fetch + OpenAI transcription uploads
defusedxml>=0.7.1
